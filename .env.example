DB_PASSWORD=changeme_use_a_strong_password
# Local dev only (ADR-0003): identity used when no X-authentik-email header is
# present (i.e. no Authentik in front). MUST be unset in production.
DEV_AUTH_EMAIL=dev@localhost
